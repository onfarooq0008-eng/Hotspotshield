{
  "name": "easyvpn-agent",
  "version": "1.0.0",
  "description": "Runs on each VPS; adds WireGuard peers on command from the EasyVPN control API.",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
